import React, { Component } from 'react';

export default class Saved extends Component {
    render() {
        return (
            <div>
                <h1>Saved</h1>
            </div>
        )
    }
}
